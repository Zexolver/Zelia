# Small brain / router / tools
ollama
requests
beautifulsoup4
pyyaml
psutil
numpy
setuptools<81   # webrtcvad imports pkg_resources at import time; setuptools>=81 dropped it entirely

# Speech
faster-whisper
piper-tts
sounddevice
webrtcvad
openwakeword
pvporcupine
evdev

# Screen / vision
pillow
pytesseract
pygobject   # AT-SPI accessibility-tree access (atspi_tool.py) -- needs at-spi2-core installed system-wide too

# Browser reading (cdp_reader.py) -- talks to Brave's remote-debugging
# protocol directly instead of OCR+synthetic scrolling
websocket-client

# Second brain (long-term memory)
chromadb
onnxruntime   # backs chromadb's DefaultEmbeddingFunction (ONNX MiniLM-L6-v2) -- see src/memory/second_brain.py

# Large brain
airllm
bitsandbytes  # needed for AirLLM's compression="4bit"/"8bit" -- without it, from_pretrained() fails outright once compression is actually requested (confirmed live)
torch
